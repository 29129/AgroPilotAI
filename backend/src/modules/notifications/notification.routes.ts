import { Router } from 'express'; import { z } from 'zod'; import { authenticate } from '../../common/middleware/auth.js'; import { asyncHandler } from '../../common/utils/async-handler.js'; import { ok } from '../../common/http/response.js'; import { prisma } from '../../database/prisma.js'; import { sendEmail } from '../../integrations/notifications.service.js';
export const notificationRouter = Router(); notificationRouter.use(authenticate);
notificationRouter.get('/', asyncHandler(async (req,res)=>ok(res,await prisma.notification.findMany({where:{userId:req.user!.id},orderBy:{createdAt:'desc'}}))));
notificationRouter.post('/', asyncHandler(async (req,res)=>{const input=z.object({title:z.string().min(1),message:z.string().min(1)}).parse(req.body); const notification=await prisma.notification.create({data:{userId:req.user!.id,...input}}); const user=await prisma.user.findUnique({where:{id:req.user!.id}}); if(user) await sendEmail(user.email,input.title,input.message).catch(()=>false); return ok(res,notification,201)}));
